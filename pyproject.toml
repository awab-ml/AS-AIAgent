[project]
name = "as-aiagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.20",
    "openai>=1.0",
    "prompt-toolkit>=3.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "rich>=13.0",
]
